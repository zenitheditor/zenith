# <Brand> brand

Project brand brief for the Zenith agent. Place at `.zenith/brand.md`. The `zenith` skill reads
this before authoring and conforms to it. Keep it concrete and token-oriented.

## Palette (roles, not just hues)

- color.brand = #**\_\_** (primary brand color)
- color.accent = #**\_\_** (secondary / highlight)
- color.ink = #**\_\_** (primary text)
- color.bg = #**\_\_** (default background)
- color.muted = #**\_\_** (secondary text / lines)

## Typography

- Headings: Noto Sans bold
- Body: Noto Sans
- Scale (px): h1=64, h2=40, body=28 (adjust to taste)

## Spacing & layout

- Spacing unit: 8px (use multiples)
- Page safe margin: 64px
- One accent emphasis per view; generous whitespace.

## Voice

- Tone: <e.g. confident, plain, warm>
- Avoid: <e.g. exclamation marks, jargon>

## Do / Don't

- Do: <e.g. tokenize every color; keep ≤ 2 fonts; high text contrast>
- Don't: <e.g. gradients on body text; raw hex in nodes; clutter the safe area>

## Assets

- Logo: assets/logo.png (declare with sha256 for locked renders)
